import 'package:flutter/material.dart';

class FishDetailPage extends StatelessWidget {
  const FishDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Fish Detail'),
      ),
    );
  }
}
